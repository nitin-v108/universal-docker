# Optional: custom Nginx image (docker-compose uses nginx:1.26-alpine directly with mounted configs)
# Use this if you need to bake configs into the image instead of mounting.
FROM nginx:1.26-alpine
# Configs are mounted at runtime; no COPY needed for default setup.
EXPOSE 80 443
